<div>
  <h1>{{title}}</h1>
</div>

<div>
  <a href="/users/{{title}}/posts/new"><button>+</button></a>
</div>

<div>
  {{#each posts as |post|}}
    <div>
      <h3>{{post.title}}</h3>
      <p>{{post.body}}</p>
    </div>
  {{/each}}
</div>
